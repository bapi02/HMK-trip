rules_version = '2';
// 데모용 규칙 — 누구나 trips 읽기/쓰기 가능. 실서비스에서는 인증 기반으로 강화할 것.
service cloud.firestore {
  match /databases/{database}/documents {
    match /trips/{tripId} {
      allow read, write: if true;
    }
  }
}
